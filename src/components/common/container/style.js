import styled from 'styled-components'
import { breakPoints } from '../../../styles/config/media-queries'

export const ContainerStyled = styled.div`
  max-width: ${breakPoints.maxContainerWidth};
  min-height: 75vh;
`
