import styled from 'styled-components'

export const AlertContainer = styled.ul`
  width: fit-content;
  height: fit-content;
  position: fixed;
  bottom: 0;
  right: 50%;
  transform: translateX(50%);
  z-index: 10000;
`
