import styled from 'styled-components'

export const AlertContainer = styled.ul`
  width: fit-content;
  height: fit-content;
  position: fixed;
  top: 0;
  left: 50%;
  transform: translate(-50%);
`
