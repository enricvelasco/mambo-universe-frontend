import styled from 'styled-components'

export const AlertContainer = styled.ul`
  width: fit-content;
  height: fit-content;
  position: fixed;
  top: 50%;
  left: 0;
  transform: translateY(-50%);
`
